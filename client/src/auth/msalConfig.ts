/**
 * MSAL (Microsoft Authentication Library) – 無効化済み。
 * 認証は Windowsユーザー名 (X-Windows-Username ヘッダ) で行う。
 */

export const DEV_AUTH_BYPASS = true;

export const msalInstance = null;
export const loginRequest = {};
export const msalConfig = {};
